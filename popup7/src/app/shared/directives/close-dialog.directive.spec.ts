import { CloseDialogDirective } from './close-dialog.directive';

describe('CloseDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseDialogDirective();
    expect(directive).toBeTruthy();
  });
});
